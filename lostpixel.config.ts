// Lost Pixel discovers this canonical filename. Keep the existing project
// configuration as the source of truth for compatibility with older tooling.
export { default } from './lost-pixel.config';
